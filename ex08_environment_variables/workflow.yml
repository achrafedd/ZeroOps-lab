name: workflow
on: [push, pull_request]
branches: [main]

jobs:
  workflow:
    env:
      PROJECT_NAME: "ZeroOps-lab"
      AUTHOR: "Achraf Eddiba"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "$PROJECT_NAME by $AUTHOR"

